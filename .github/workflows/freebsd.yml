name: FreeBSD

# spell-checker:ignore sshfs usesh vmactions taiki Swatinem esac fdescfs fdesc sccache nextest copyback logind

env:
  # * style job configuration
  STYLE_FAIL_ON_FAULT: true ## (bool) fail the build if a style job contains a fault (error or warning); may be overridden on a per-job basis

on:
  pull_request:
  push:
    #branches:
    #  - '*'

permissions:
  contents: read # to fetch code (actions/checkout)

# End the current execution if there is a new changeset in the PR.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  style:
    name: Style and Lint
    runs-on: ${{ matrix.job.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        job:
            - { os: ubuntu-24.04 , features: unix }
    steps:
    - uses: actions/checkout@v6
      with:
        persist-credentials: false
    - name: Prepare, build and test
      uses: vmactions/freebsd-vm@v1.2.9
      with:
        usesh: true
        sync: rsync
        copyback: false
        # We need jq and GNU coreutils to run show-utils.sh and bash to use inline shell string replacement
        prepare: pkg install -y curl sudo jq coreutils bash cmake perl5 p5-App-cpanminus curl ca_root_nss
        run: |
          sudo -i bash << EOF
          pkg update
          pkg upgrade
          perl --version
          cpanm --notest Devel::CheckLib JSON::MaybeXS Test2::V0 Test::Pod Test::Pod::Coverage Test::CPAN::Meta
          pkg install jsonnet
          jsonnet --version
          perl Makefile.PL
          make
          make test
          EOF

 
